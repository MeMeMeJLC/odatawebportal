@model IEnumerable<refwebportal.Penalty>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PenaltyTypeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PenaltyTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GamePlayer.Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PenaltyTypeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PenaltyTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GamePlayer.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
